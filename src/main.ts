import './style.css';

// const name: string = 'Bob';
// const lastName: string = 'Pes';
// const age: number = 19;
// const status: boolean = true;
// const budjet: null = null;
// const statusQ: undefined = undefined;

//todo Create a TypeScript function named displayUserProfile that takes a user profile object as an argument. The user profile object should have the following properties: username (string), age (number), and isActive (boolean). The function should return a string summarizing the user's profile details.

// type userProfile = {
//   userName: string;
//   age: number;
//   isActive: boolean;
// };

// const user = {
//   userName: 'Bob',
//   age: 12,
//   isActive: true,
// };

// function displayUserProfile(user: userProfile) {
//   return `User name is ${user.userName} his is ${user.age} years old and his is ${user.isActive}`;
// }

// console.log(displayUserProfile(user));

// function logName(name: string) {
//   return name;
// }
// console.log(logName('pes'));

// function calcTotal(arr: { asd: number }[]) {
//   return arr.reduce((acc, el) => acc + el.asd, 10);
// }
// const numbers = [{ asd: 10 }, { asd: 20 }, { asd: 30 }, { asd: 40 }];
// console.log(calcTotal(numbers));
